{
  "name": "kuria",
  "version": "0.1.0",
  "devDependencies": {
    "lerna": "^3.3.2",
    "npm-run-all": "^4.1.3"
  },
  "scripts": {
    "lerna": "lerna",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Rassilion/Kuria.git"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "server",
      "web"
    ]
  }
}
